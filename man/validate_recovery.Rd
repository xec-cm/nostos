% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_recovery.R
\name{validate_recovery}
\alias{validate_recovery}
\title{Validate registration structure, dependencies and historical scope}
\usage{
validate_recovery(tse, analysis_id = NULL)
}
\arguments{
\item{tse}{A \link[TreeSummarizedExperiment:TreeSummarizedExperiment]{TreeSummarizedExperiment::TreeSummarizedExperiment} containing
zero or more registrations created with \code{\link[=setup_recovery]{setup_recovery()}}. Subsets with
no current samples or features are supported for historical validation.}

\item{analysis_id}{\code{NULL} to inspect all named analyses, or one analysis ID
matching \verb{^[a-z][a-z0-9]*$}. An unknown name produces a report finding.}
}
\value{
A plain list with \code{report_schema_version = 1L}, and two
\link[S4Vectors:DataFrame]{S4Vectors::DataFrame} components, including typed empty tables:
\describe{
\item{summary}{One row per selected readable analysis: \code{analysis_id},
\code{structural_valid}, \code{validation_complete}, \code{dependencies},
\code{sample_scope}, \code{feature_scope}, \code{n_registered}, and \code{n_retained}.}
\item{diagnostics}{Findings with \code{analysis_id} (\code{NA} for global findings),
\code{code}, \code{severity}, \code{component}, \code{message}, and an \code{ids} list-column
containing complete character vectors of affected identities.}
}
}
\description{
Diagnose named registrations against the current TreeSummarizedExperiment
without changing the object, rewriting history, or reading assay values.
}
\details{
Interpret the summary fields together. \code{structural_valid} is \code{FALSE} for a
demonstrated broken structure, \code{TRUE} for a fully understood valid structure,
and \code{NA} when an unsupported schema or stage prevents that conclusion.
\code{validation_complete} records whether all required checks could run.
Unsupported records never receive a complete validation pass; other named
analyses are checked independently.

\code{dependencies} is \code{"unchanged"}, \code{"changed"}, or \code{"not_checked"}. Comparisons
use IDs and the consumed source columns of retained original samples. Allowed
factors and integer times are normalized for comparison. A retained excluded
sample remains excluded only while its episode is \code{NA}; its subject and time
values are not dependencies. Removing a sample does not prove anything about
its unavailable metadata. If no originally included sample remains,
dependencies are \code{"not_checked"} unless a change is established independently.

Each scope is \code{"same"} (including reordering), \code{"subset"}, \code{"expanded"},
\code{"mixed"}, \code{"empty"}, or \code{"not_checked"}. Scope compares current IDs with the
full original container, including originally excluded samples. New IDs are
not enrolled. \code{n_registered} and \code{n_retained} count originally included samples;
unknown counts use \code{NA_integer_}.

Episode and event references are checked against the historical snapshot.
Filtering away all observations of an episode does not break that history.
No record is repaired, sorted, recomputed, or timestamped by validation.
Registration validation does not inspect assay values or certify analytical
eligibility, baseline estimates, deviations, or recovery outcomes. Populated
analytical stages require a later validator and produce \code{STAGE_UNSUPPORTED}.

Diagnostic codes, column types and enumerated states are stable contracts;
message wording and diagnostic order are not. Severity describes an individual
finding and is not an overall success flag. Invalid argument types or syntax
raise \code{recoverome_error_input} through \code{\link[cli:cli_abort]{cli::cli_abort()}}. Missing analyses,
malformed stored records and formal S4 validity failures are report findings.
}
\examples{
data("recovery_examples", package = "recoverome")
example_data <- recovery_examples$single_episode
tse <- TreeSummarizedExperiment::TreeSummarizedExperiment(
  assays = list(counts = example_data$counts),
  colData = S4Vectors::DataFrame(example_data$col_data)
)
registered <- setup_recovery(
  tse,
  analysis_id = "antibiotic",
  episodes = example_data$episodes,
  events = example_data$events,
  time_col = example_data$time_col,
  time_unit = example_data$time_unit,
  time_origin = example_data$time_origin
)

validate_recovery(registered)$summary
validate_recovery(registered[, c(1, 3)])$diagnostics
}
\seealso{
\code{\link[=setup_recovery]{setup_recovery()}}
}
